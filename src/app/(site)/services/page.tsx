import { RouteStub } from "@/components/shared/RouteStub";

export default function ServicesPage() {
  return (
    <RouteStub
      eyebrow="Services"
      title="Services"
      description="A future page for founder-oriented build requests, collaboration paths, and selected ways to work together."
    />
  );
}
