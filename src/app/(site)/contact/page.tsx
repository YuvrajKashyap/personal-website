import { RouteStub } from "@/components/shared/RouteStub";

export default function ContactPage() {
  return (
    <RouteStub
      eyebrow="Get in Touch"
      title="Contact"
      description="A future page for reaching out with opportunities, collaborations, and focused messages."
    />
  );
}
