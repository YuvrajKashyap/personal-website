import { RouteStub } from "@/components/shared/RouteStub";

export default function TrackerPage() {
  return (
    <RouteStub
      eyebrow="Tracker"
      title="Tracker"
      description="A future page for visualizing goals, inputs, clarity, and personal operating metrics."
    />
  );
}
